<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 권한 선언 (정리된 버전) -->
    <!-- 네트워크 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Wi-Fi 상태 확인을 위한 퍼미션 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <!-- Wi-Fi 스캔을 위한 퍼미션 (Android 13 이상에서 필요) -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- 위치 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 센서 및 활동 -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" tools:ignore="HighSamplingRate" />

    <!-- 포그라운드 서비스 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- 알림 및 부팅 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 미디어 및 저장소 (버전별 관리) -->
    <!-- Android 13 (API 33) 이상 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 14 (API 34) 이상 -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- Android 12L (API 32) 이하 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- 기타 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 하드웨어 기능 명시 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/img_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/img_logo"
        android:supportsRtl="true"
        android:forceDarkAllowed="false"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.MyApplication12345"
        tools:targetApi="34"> <!-- targetSdk 버전에 맞게 업데이트 -->
        <profileable android:shell="true" />
        <!-- 1. 앱의 유일한 시작점: SplashActivity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting"> <!-- SplashScreen 테마 적용 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. IntroActivity는 더 이상 LAUNCHER가 아니며, 다른 앱에서 호출할 필요 없으므로 exported="false" -->
        <activity
            android:name=".ui.login.IntroActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication12345.Auth" />

        <!-- 나머지 액티비티들은 exported="false" (다른 앱에서 호출할 필요 없음) -->
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".ui.login.JoinActivity" android:theme="@style/Theme.MyApplication12345.Auth" android:exported="false" />
        <activity android:name=".chatbot.ChatbotUi" android:exported="false" />
        <activity android:name=".ui.camera.ResultActivity" android:exported="false" />
        <activity android:name=".ui.sidebar.foodcalculator.FoodCalculatorActivity" android:exported="false" />
        <activity android:name=".ui.sidebar.carbonquiz.QuizActivity" android:exported="false" />
        <activity android:name=".ui.sidebar.setting.SettingActivity" android:exported="false" />


        <!-- 서비스 및 리시버 -->
        <service
            android:name=".AI.SensorDataService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location|dataSync"
            tools:ignore="ForegroundServicePermission" />

        <receiver
            android:name=".AlarmBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 메타 데이터 -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <meta-data
            android:name="com.example.myapplication12345.OPENAI_API_KEY"
            android:value="${OPENAI_API_KEY}" />

    </application>

</manifest>